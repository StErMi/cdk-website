// import { Stack } from '@aws-cdk/core';
import '@aws-cdk/assert/jest';
// import { Website, WebsiteProps } from '../src/website';

test('Custom Construct Test', () => {
  // const props = {
  //   env: {
  //     account: 'XXXXXXXXXXX',
  //     region: 'eu-west-1',
  //   },
  // };
  // const websiteProps: WebsiteProps = {
  //   indexDoc: 'index.html',
  //   errorDoc: 'index.html',
  //   websiteBuildFolder: '../build/',
  //   domain: 'amazon.com',
  //   subdomain: 'cdk',
  // };

  // const stack = new Stack(undefined, 'TestStack', props);
  // new Website(stack, 'NewWebsiteDeployment', websiteProps).deploy();

  // expect(stack).toHaveResource('AWS::S3::Bucket');
  // expect(stack).toHaveResource('AWS::CloudFront::Distribution');
});
